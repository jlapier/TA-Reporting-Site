<h1>Previewing Summary for <%= @report.name %></h1>
<span class="fake_button">
  <%= link_to("< Search Results", activities_path(:activity_search => params[:activity_search])) %>
</span>
<%= render :partial => 'summary' %>